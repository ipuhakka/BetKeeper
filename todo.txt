1. Change statistics so that on receiving all folders, along with calculating overview values, it calculates
statistics for each folder and updates a state folderStatistics. These can be used in rendering all graphOptions
and tables.

2. Find out why sagas.js- fetchBetsFromAllFolders gives cannot read property undefined
when action.payload is not checked (something to do with watch function).
